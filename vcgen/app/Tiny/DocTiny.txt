The Language Tiny
BNF Converter


%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of Tiny==
===Identifiers===
Identifiers //Ident// are unquoted strings beginning with a letter,
followed by any combination of letters, digits, and the characters ``_ '``
reserved words excluded.


===Literals===
Integer literals //Integer// are nonempty sequences of digits.




===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in Tiny are the following:
  | ``begin`` | ``call`` | ``do`` | ``else``
  | ``end`` | ``exists`` | ``false`` | ``fi``
  | ``forall`` | ``if`` | ``invariant`` | ``postcondition``
  | ``precondition`` | ``proc`` | ``skip`` | ``then``
  | ``true`` | ``var`` | ``while`` |

The symbols used in Tiny are the following:
  | ; | := | ( | )
  | = | { | } | ==>
  | . | /\ | ∧ | \/
  | ∨ | <= | ≤ | >
  | ~ | ¬ | + | -
  | * | / |  |

===Comments===
Single-line comments begin with //.There are no multiple-line comments in the grammar.

==The syntactic structure of Tiny==
Non-terminals are enclosed between < and >.
The symbols -> (production),  **|**  (union)
and **eps** (empty rule) belong to the BNF notation.
All other symbols are terminals.

  | //Stmt// | -> | //Stmt1// ``;`` //Stmt//
  |  |  **|**  | //Stmt1//
  | //Stmt1// | -> | //Ident// ``:=`` //Expr//
  |  |  **|**  | ``skip``
  |  |  **|**  | //Stmt2//
  | //Stmt2// | -> | ``if`` //BExpr// ``then`` //Stmt// ``else`` //Stmt1//
  |  |  **|**  | ``if`` //BExpr// ``then`` //Stmt// ``fi``
  |  |  **|**  | ``while`` //BExpr// ``do`` //Stmt1//
  |  |  **|**  | ``while`` //BExpr// ``do`` //[Invariant]// ``;`` //Stmt1//
  |  |  **|**  | ``call`` //Ident// ``(`` //Expr// ``)``
  |  |  **|**  | ``begin`` //Decl// ``;`` //Stmt// ``end``
  |  |  **|**  | ``begin`` //Stmt// ``end``
  | //[Invariant]// | -> | **eps**
  |  |  **|**  | //Invariant//
  |  |  **|**  | //Invariant// ``;`` //[Invariant]//
  | //Invariant// | -> | ``invariant`` //Formula//
  | //Decl1// | -> | ``var`` //Ident// ``=`` //Expr//
  |  |  **|**  | ``proc`` //Ident// ``(`` //Ident// ``)`` ``{`` //Stmt// ``}``
  |  |  **|**  | ``proc`` //Ident// ``(`` //Ident// ``)`` //[SpecEl]// ``{`` //Stmt// ``}``
  | //Decl// | -> | //Decl// ``;`` //Decl1//
  |  |  **|**  | //Decl1//
  | //[SpecEl]// | -> | **eps**
  |  |  **|**  | //SpecEl//
  |  |  **|**  | //SpecEl// ``;`` //[SpecEl]//
  | //SpecEl// | -> | ``precondition`` //Formula//
  |  |  **|**  | ``postcondition`` //Formula//
  | //Formula1// | -> | //FormulaD//
  |  |  **|**  | //Formula// ``==>`` //Formula//
  |  |  **|**  | //[Binder]// ``.`` //BExpr// ``==>`` //Formula//
  |  |  **|**  | //[Binder]// ``.`` //BExpr//
  |  |  **|**  | //Formula1// ``/\`` //Formula//
  |  |  **|**  | //Formula1// ``∧`` //Formula//
  |  |  **|**  | ``(`` //Formula// ``)``
  | //FormulaD// | -> | //BExpr// ``\/`` //FormulaD//
  |  |  **|**  | //BExpr// ``∨`` //FormulaD//
  |  |  **|**  | //BExpr//
  | //Formula// | -> | //Formula1//
  | //[Binder]// | -> | **eps**
  |  |  **|**  | //Binder//
  |  |  **|**  | //Binder// ``.`` //[Binder]//
  | //Binder// | -> | ``forall`` //Ident//
  |  |  **|**  | ``exists`` //Ident//
  | //BExpr// | -> | //BExpr// ``∧`` //BExpr1//
  |  |  **|**  | //BExpr// ``/\`` //BExpr1//
  |  |  **|**  | //BExpr1//
  | //BExpr1// | -> | //Expr// ``=`` //Expr//
  |  |  **|**  | //Expr// ``<=`` //Expr//
  |  |  **|**  | //Expr// ``≤`` //Expr//
  |  |  **|**  | //Expr// ``>`` //Expr//
  |  |  **|**  | ``~`` //BExpr1//
  |  |  **|**  | ``¬`` //BExpr1//
  |  |  **|**  | ``true``
  |  |  **|**  | ``false``
  |  |  **|**  | ``(`` //BExpr// ``)``
  | //Expr// | -> | //Expr// ``+`` //Expr1//
  |  |  **|**  | //Expr// ``-`` //Expr1//
  |  |  **|**  | //Expr1//
  | //Expr1// | -> | //Expr1// ``*`` //Expr2//
  |  |  **|**  | //Expr1// ``/`` //Expr2//
  |  |  **|**  | //Expr2//
  | //Expr2// | -> | //Integer//
  |  |  **|**  | //Ident//
  |  |  **|**  | ``(`` //Expr// ``)``



%% File generated by the BNF Converter (bnfc 2.9.6.1).
