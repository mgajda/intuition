## File generated by the BNF Converter (bnfc 2.9.5).

# Makefile for building the parser and test program.

BNFC       = bnfc
GHC        = ghc
HAPPY      = happy
HAPPY_OPTS = --array --info --ghc --coerce
ALEX       = alex
ALEX_OPTS  = --ghc

# List of goals not corresponding to file names.

TARGETS = Tiny/Test Interpreter

.PHONY : all clean distclean

# Default goal.

all : ${TARGETS}

# Rules for building the parser.

%.hs : %.y
	${HAPPY} ${HAPPY_OPTS} $<

%.hs : %.x
	${ALEX} ${ALEX_OPTS} $<

Tiny/Abs.hs: Tiny.cf
	${BNFC} -d $<


Tiny/Test : Tiny/Abs.hs Tiny/Lex.hs Tiny/Par.hs Tiny/Print.hs Tiny/Test.hs
	${GHC} ${GHC_OPTS} $@


Interpreter : Tiny/Abs.hs Tiny/Lex.hs Tiny/Par.hs Tiny/Print.hs Interpreter.hs
	${GHC} ${GHC_OPTS} $@


# Rules for cleaning generated files.

clean :
	-rm -f *.hi *.o *.log *.aux *.dvi ${TARGETS} Tiny/* *~ *.bak
	-rmdir Tiny


# EOF
