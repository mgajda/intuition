--- a/vcgen/vcgen.cabal
+++ b/vcgen/vcgen.cabal
@@ -65,6 +65,7 @@ custom-setup
 test-suite vcgen-tests
   type:                exitcode-stdio-1.0
   hs-source-dirs:      test, app, app/Tiny
   main-is:             Specs.hs
+  other-modules:       AbsTiny, Logic, SkelTiny
   build-depends:       base >=4.14 && <5,
                        QuickCheck,
                        tasty,
